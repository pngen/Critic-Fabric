# Build trees. Nothing generated by a build belongs in the repository.
build*/
cmake-build-*/

# The downstream consumer is a standalone project with its own build directory.
downstream/build*/

# Durable review journals produced by running the runtime.
*.journal
*.journal.tmp

# Editor and platform debris.
.vs/
.vscode/
.idea/
*.user
*.swp
*~
Thumbs.db
desktop.ini
